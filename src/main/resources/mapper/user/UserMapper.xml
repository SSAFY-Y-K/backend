<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.passit.user.repository.UserRepository">

    <insert id="insert" parameterType="SignupRequest">
        INSERT INTO users (username, password, nickname)
            VALUES (#{username},
                    #{password},
                    #{nickname});
    </insert>

    <select id="countByUsername" parameterType="String">
        SELECT
            COUNT(*)
        FROM users
        WHERE username = #{username};
    </select>

    <select id="countByNickname" parameterType="String">
        SELECT
            COUNT(*)
        FROM users
        WHERE nickname = #{nickname};
    </select>
</mapper>
